spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/webdb3
    username: root
    password:

logging:
  level:
    org:
      springframework: info
      webdev: debug